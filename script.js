const numbers = document.querySelectorAll("[data-number]"); // check
const operations = document.querySelectorAll("[data-operation]"); // check
const clear = document.querySelectorAll("[data-clear]"); // check
const delNumber = document.querySelectorAll("[data-del]"); // check
const result = document.querySelectorAll("[data-result]"); // check

const previous = document.querySelectorAll("[data-previous]");
const current = document.querySelectorAll("[data-current]");
console.log(previous,current);